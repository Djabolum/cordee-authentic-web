module.exports = {
  // PostCSS config — includes Tailwind and Autoprefixer.
  // Tailwind is added as a devDependency in package.json.
  plugins: {
    tailwindcss: {},
    autoprefixer: {},
  },
};
