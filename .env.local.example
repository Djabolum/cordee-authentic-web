# Example environment variables for Cordée Authentique
# Copy this file to `.env.local` and adjust values for your environment.

# REST endpoint returning latest field value (JSON with `resonance` and `intent`)
NEXT_PUBLIC_FIELD_REST_URL=http://fieldbridge-production.up.railway.app/field/last

# WebSocket endpoint for real-time field updates (wss://...)
# Use the fieldbridge WS (bridge) in production
NEXT_PUBLIC_FIELD_WS_URL=wss://fieldbridge-productions.up.railway.app/ws/field